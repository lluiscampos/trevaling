language: node_js
node_js:
  - "4.2"

before_install:
  - npm i -g npm@3
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-test-dev lcov vera++

script:
  - make build
  - make lint
  - make test
  - make coveralls
