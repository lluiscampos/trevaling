language: generic
sudo: required
dist: trusty

before_install:
  - npm i -g npm@3
  - sudo apt-get -qq update
  - sudo apt-get install -y libboost-test-dev lcov vera++

script:
  - make build
  - make lint
  - make test
  - make coveralls
